/**
 * Centralized constants for MVP demand behavior.
 */

/**
 * The MVP access radius for capturing demand nodes from a stop.
 * Only demand nodes strictly within or equal to this distance from a stop are captured.
 */
export const STOP_ACCESS_RADIUS_METERS = 400;
